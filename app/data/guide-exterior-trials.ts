// Online viewers cannot replace an exterior that must retain its OSM context.
// Florence's scan remains on /models#florence-duomo until an asset can be composed.
export const guideExteriorTrials: Readonly<Record<string, string>> = {};
